services:
  php:
    environment:
      APP_ENV: prod
      APP_SECRET: ${APP_SECRET}
    # Esegue le migrazioni del DB e pulisce la cache all'avvio
    command: >
      sh -c "php bin/console doctrine:migrations:migrate --no-interaction &&
             php bin/console cache:clear &&
             php-fpm"
